org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=384m -XX:ReservedCodeCacheSize=128m -XX:+HeapDumpOnOutOfMemoryError
# Keep Android builds reliable on store-admin laptops with limited RAM.
org.gradle.parallel=false
org.gradle.workers.max=1
# Avoid a second Kotlin JVM competing with Gradle for RAM during APK builds.
kotlin.compiler.execution.strategy=in-process
kotlin.incremental=false
org.gradle.caching=true
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

